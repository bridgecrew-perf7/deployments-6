<?xml version="1.0" encoding="UTF-8"?>

<configuration>
{% set dict = hive_site_config|combine(hive_site_config_defaults) %}
  {% for key,value in dict.items() %}
    <property>
        <name>{{ key }}</name>
        <value>{{ value }}</value>
    </property>
  {% endfor %}
</configuration>

