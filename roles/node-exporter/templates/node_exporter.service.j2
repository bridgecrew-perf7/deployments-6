[Unit]
Description=Node Exporter
After=network.target
 
[Service]
User=node_exporter
Group=node_exporter
Type=simple
ExecStart=/usr/local/bin/node_exporter --web.listen-address="{{ inventory_hostname }}:9100"
Restart=always

[Install]
WantedBy=multi-user.target